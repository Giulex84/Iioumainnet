const PI_API_BASE='https://api.minepi.com/v2';
function getBearerToken(req){const h=req.headers.authorization||'';return h.startsWith('Bearer ')?h.slice(7).trim():null}
export async function verifyPiUser(req){const token=getBearerToken(req);if(!token){const e=new Error('Missing Pi access token');e.status=401;throw e}const r=await fetch(`${PI_API_BASE}/me`,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok){const e=new Error('Invalid or expired Pi session');e.status=401;throw e}const u=await r.json();if(!u?.uid||!u?.username){const e=new Error('Pi identity response is incomplete');e.status=401;throw e}return u}
export async function piServerRequest(path,options={}){const apiKey=process.env.PI_API_KEY;if(!apiKey){const e=new Error('PI_API_KEY is not configured');e.status=503;throw e}const r=await fetch(`${PI_API_BASE}${path}`,{...options,headers:{Authorization:`Key ${apiKey}`,...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}});const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{data={raw:text}}if(!r.ok){const e=new Error(data?.error||data?.message||`Pi API error ${r.status}`);e.status=r.status;e.data=data;throw e}return data}
export function apiError(res,error){const status=Number(error?.status)||500;const safe=status<500||status===503?error.message:'Server error';return res.status(status).json({success:false,error:safe,...(process.env.NODE_ENV!=='production'&&status>=500?{detail:error.message}:{})})}
